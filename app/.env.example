# Copy to .env.local and fill in values from Supabase dashboard → Settings → API.
# Required for Google Sign-In restricted to @stanford.edu.

SUPABASE_URL=
SUPABASE_ANON_KEY=
APP_URL=http://localhost:3000
